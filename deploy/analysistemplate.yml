---
apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: webcheck
spec:
  metrics:
  - name: webcheck
    successCondition: "true"
    count: 30
    interval: 2m
    provider:
      web:
        # paceholders are resolved when an AnalysisRun is created
        url: "http://51.159.24.218/"
        jsonPath: "{$.ok}"
